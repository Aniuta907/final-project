const brand = {
	color: 'Aquamarine'
};

const item = {
	color: 'Aquamarine'
};

const menu = {
	height: '60px'
};

const styles = {
	brand: brand,
	item: item,
	menu: menu
};

export default styles;
